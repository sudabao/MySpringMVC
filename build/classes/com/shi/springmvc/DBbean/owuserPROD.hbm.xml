<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC 
 "-//Hibernate/Hibernate Mapping DTD//EN"
 "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd"> 

<hibernate-mapping>
	<!-- 1 配置类和表对应   
        class标签  
        name属性：实体类全路径  
        table属性：数据库表名称  
    -->
   <class name="com.shi.springmvc.DBbean.owuserPROD" table="owuserPROD">
	<!-- 2 配置“主键”的映射
            id标签
            name属性：实体类里面id属性名称
            column属性：生成的表字段名称
            type属性：该字段的数据类型
        -->
      <id name="idname" type="string" column="idname">
      	<!-- 设置主键的增长方法
                increment（递增）
                identity (标识)
                sequence （序列）
                hilo （高低位）
                seqhilo（使用序列的高低位）
                native（本地）
                assigned（设置）
            -->
      </id>
      <!-- 配置其他属性和表字段对应
            name属性：实体类属性名称
            column属性：生成表字段名称
            type属性：该字段的数据类型
        -->
      <property name="mainhero" column="mainhero" type="string"/>
      <property name="finalword" column="finalword" type="string"/>
      <property name="time" column="time" type="string"/>
   </class>
</hibernate-mapping>